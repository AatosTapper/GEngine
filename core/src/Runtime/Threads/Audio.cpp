#include "Audio.h"

#include "../../Util/Util.h"

namespace geng
{
    void audio_thread(AudioThreadData *audio_thread_data, std::atomic<bool> &stop_threads)
    {
        LOG("Audio Thread Run");
    }
}