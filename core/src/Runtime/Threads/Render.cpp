#include "Render.h"

#include "../../Util/Util.h"

namespace geng
{
    void render_thread(RenderThreadData *render_thread_data, std::atomic<bool> &stop_threads)
    {
        LOG("Render Thread Run");
    }
}